name: Publicar Historias

on:
  workflow_dispatch:
    inputs:
      cuenta:
        description: 'Cuenta a correr (curious4d, canal2, oscuro4d, vive4d, o vacio)'
        required: false
        default: ''
  schedule:
    - cron: '0 9 * * *'
    - cron: '4 10 * * *'
    - cron: '46 10 * * *'
    - cron: '49 11 * * *'
    - cron: '32 12 * * *'
    - cron: '35 13 * * *'
    - cron: '18 14 * * *'
    - cron: '21 15 * * *'
    - cron: '4 16 * * *'
    - cron: '7 17 * * *'
    - cron: '11 18 * * *'
    - cron: '53 18 * * *'
    - cron: '56 19 * * *'
    - cron: '39 20 * * *'
    - cron: '42 21 * * *'
    - cron: '28 23 * * *'
    - cron: '11 0 * * *'
    - cron: '14 1 * * *'
    - cron: '56 1 * * *'
    - cron: '0 3 * * *'

concurrency:
  group: stories
  cancel-in-progress: false

jobs:
  publicar-historias:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    steps:
      - name: Descargar el codigo
        uses: actions/checkout@v4

      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci || npm install

      - name: Instalar fuente para los titulos
        run: sudo apt-get update && sudo apt-get install -y fonts-liberation fonts-montserrat

      - name: Reconstruir credenciales desde Secrets
        env:
          ACCOUNTS_JSON: ${{ secrets.ACCOUNTS_JSON }}
        run: printf '%s' "$ACCOUNTS_JSON" > accounts.json

      - name: Generar y publicar Historias
        env:
          DRY_RUN: ${{ vars.DRY_RUN || 'false' }}
          TIKTOK_ENABLED: ${{ vars.TIKTOK_ENABLED || 'true' }}
          AGNES_API_KEY: ${{ secrets.AGNES_API_KEY }}
          CRON: ${{ github.event.schedule }}
          CUENTA_MANUAL: ${{ inputs.cuenta }}
        run: |
          if [ -n "$CUENTA_MANUAL" ]; then
            CUENTA="$CUENTA_MANUAL"
          else
            case "$CRON" in
              "0 9 * * *")  CUENTA="curious4d" ;;
              "4 10 * * *")  CUENTA="canal2" ;;
              "46 10 * * *")  CUENTA="oscuro4d" ;;
              "49 11 * * *")  CUENTA="vive4d" ;;
              "32 12 * * *")  CUENTA="curious4d" ;;
              "35 13 * * *")  CUENTA="canal2" ;;
              "18 14 * * *")  CUENTA="oscuro4d" ;;
              "21 15 * * *")  CUENTA="vive4d" ;;
              "4 16 * * *")  CUENTA="curious4d" ;;
              "7 17 * * *")  CUENTA="canal2" ;;
              "11 18 * * *")  CUENTA="oscuro4d" ;;
              "53 18 * * *")  CUENTA="vive4d" ;;
              "56 19 * * *")  CUENTA="curious4d" ;;
              "39 20 * * *")  CUENTA="canal2" ;;
              "42 21 * * *")  CUENTA="oscuro4d" ;;
              "28 23 * * *")  CUENTA="vive4d" ;;
              "11 0 * * *")  CUENTA="curious4d" ;;
              "14 1 * * *")  CUENTA="canal2" ;;
              "56 1 * * *")  CUENTA="oscuro4d" ;;
              "0 3 * * *")  CUENTA="vive4d" ;;
              *)             CUENTA="" ;;
            esac
          fi
          echo "Corriendo historia de cuenta: $CUENTA (cron: $CRON)"
          node generate-story.js "$CUENTA"

      - name: Guardar el historial de temas actualizado
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ 2>/dev/null || true
          git diff --staged --quiet && echo "Nada que guardar" && exit 0
          git commit -m "Actualizar historial de temas (historias) [skip ci]"
          git pull --rebase --autostash origin main || true
          git push || echo "No se pudo hacer push"
          echo "Historial guardado"
